@page
@model PayrollLite.Pages.MonthsOfYear.DetailModel
@{
    Layout = "_MainLayout";
    ViewData["Title"] = "Gender Type";
}

<div class="container-xxl flex-grow-1 container-p-y">
    <div class="row">
        <div class="col-xxl">
            @if (Model.Vm is null)
            {
                <div class="alert alert-danger" role="alert">
                    <p>No record with id:@Model.Id exist.</p>
                </div>
            }
            else
            {
                <h4 class="fw-bold py-3 mb-1">@ViewData["Title"]</h4>
                <div class="card mb-4">
                    <div class="card-header d-flex align-items-center justify-content-between">
                        <p class="mb-0">
                            <strong>Details </strong><br />
                        </p>
                    </div>
                    <div class="card-body">
                        <form method="post">
                            <div class="row mb-3">
                                <label asp-for="Vm.Name" class="col-sm-2 col-form-label">Name :</label>
                                <div class="col-sm-10">
                                    <input asp-for="Vm.Name" type="text" readonly class="form-control" />
                                </div>
                            </div>
                            <div class="row mb-3">
                                <label asp-for="Vm.Abbreviation" class="col-sm-2 col-form-label">Abbreviation :</label>
                                <div class="col-sm-10">
                                    <input asp-for="Vm.Abbreviation" type="text" readonly class="form-control" />
                                </div>
                            </div>
                            <div class="row mb-3">
                                <label asp-for="Vm.Number" class="col-sm-2 col-form-label">Number :</label>
                                <div class="col-sm-10">
                                    <input asp-for="Vm.Number" type="text" readonly class="form-control" />
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-sm-2"></div>
                                <div class="col-sm-10 align-content-end">
                                    <a asp-page="Index" class="btn btn-secondary"><span class="tf-icons bx bx-arrow-back"></span> Back to List</a>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
